RewriteEngine On

# لو الملف أو الفولدر موجود فعلاً، Apache يخدمه
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# غير كده، ابعت الطلب لـ Slim
RewriteRule ^ index.php [QSA,L]
